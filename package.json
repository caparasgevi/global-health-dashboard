{
  "name": "global-health-dashboard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "health-radar"
  ],
  "scripts": {
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm install && npm run build",
    "build:frontend": "cd health-radar && npm install && npm run build"
  }
}