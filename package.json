{
  "name": "global-health-dashboard",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "install-all": "npm install --prefix health-radar && npm install --prefix backend",
    "build": "npm run install-all && cd health-radar && npm run build"
  },
  "dependencies": {
    "firebase": "^12.12.1"
  }
}
